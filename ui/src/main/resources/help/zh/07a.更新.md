# 检查更新

## 详情

将使用通用的 *Windows 版 Chrome* 用户代理连接到 `https://api.github.com/repos/zapek/Xeres/releases/latest`，以避免被追踪。

获取的版本会在本地比较，如果有更新的版本，会提供下载它的选项。如果接受，将使用相同的机制从 GitHub 下载该文件及其签名。在安装之前，会使用正在运行的应用程序中内置的公钥进行签名验证。如果一切正常，就可以安装更新。

检查每天执行一次（带有偏差，以使剖析更加困难）。

## 风险

🟢 低

GitHub 可能（但不确定）知道您正在使用 Xeres。
